#include "binary_trees.h"

/**
 * binary_tree_is_avl - checks if given binary tree has valid AVL properties
 * @tree: pointer to root node of binary tree passed to check
 *
 * Return: 1 for valid AVL else 0
 */

int binary_tree_is_avl(const binary_tree_t *tree)
{
	if (!tree)
		return (0);
}
